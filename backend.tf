#configuring the backend to store the tfstate
/*
terraform {
  backend "s3" {
    region     = "eu-west-1"
    bucket     = "GIVE THE BUCKET NAME"
    key        = "terraform.tfstate"
  }
}
*/